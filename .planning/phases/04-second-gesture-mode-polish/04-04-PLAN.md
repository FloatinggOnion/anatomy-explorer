---
phase: 04-second-gesture-mode-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/components/HandStatusIndicator.tsx
  - src/components/LandmarkCanvas.tsx
  - src/App.tsx
  - vite.config.ts
autonomous: false
requirements:
  - GEST-03
  - GEST-04

must_haves:
  truths:
    - "HandStatusIndicator displays current gesture mode ('Pinch' or 'Wave') so user knows which mode is active"
    - "Permission flow, loading states, and toast messages are polished with no rough edges (D-20)"
    - "App runs at consistent frame rate during active gesture use (D-21)"
    - "Bundle is within 500KB gzipped budget (D-21)"
  artifacts:
    - path: "src/components/HandStatusIndicator.tsx"
      provides: "Gesture mode display in status indicator"
      contains: "gestureMode"
    - path: "src/App.tsx"
      provides: "Polish fixes for edge cases found during demo testing"
  key_links:
    - from: "src/components/HandStatusIndicator.tsx"
      to: "src/store/appState.ts"
      via: "useAppStore((s) => s.gestureMode)"
      pattern: "gestureMode"

user_setup: []
---

<objective>
Polish the app for capstone demo: gesture mode display in the status indicator, demo-ready UX cleanup, and performance verification.

Purpose: The demo is the final deliverable. This plan ensures the app looks professional, communicates state clearly to the audience, and runs without frame drops during a live presentation (D-18 to D-21).
Output: HandStatusIndicator updated with mode display; UX rough edges cleaned up; performance verified within 500KB gzip budget.
</objective>

<execution_context>
@/Users/paul/Documents/programming/ar-project/.planning/phases/04-second-gesture-mode-polish/04-CONTEXT.md
</execution_context>

<context>
@/Users/paul/Documents/programming/ar-project/.planning/ROADMAP.md

<interfaces>
<!-- From src/store/appState.ts -->
```typescript
gestureMode: 'pinch' | 'wave';  // Added in Plan 01
handDetected: boolean;
handTrackingReady: boolean;
gestureActive: boolean;
```

<!-- From src/components/HandStatusIndicator.tsx — read to understand current display -->
<!-- Currently shows: hand detection status, tracking ready state, error messages -->
<!-- Add gestureMode pill: "Pinch Mode" or "Wave Mode" badge next to existing status -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gesture mode display to HandStatusIndicator and audit loading/toast UX</name>
  <files>src/components/HandStatusIndicator.tsx, src/App.tsx</files>
  <read_first>
    - src/components/HandStatusIndicator.tsx — full file; understand current layout, colors, and z-index positioning
    - src/App.tsx — check toast styling added in Plan 02; verify no z-index conflict with HandStatusIndicator
    - src/components/BottomToolbar.tsx — check error toast styling for consistency reference
  </read_first>
  <action>
    In HandStatusIndicator.tsx:
    - Subscribe to `gestureMode` from useAppStore: `const gestureMode = useAppStore((s) => s.gestureMode);`
    - Add a mode badge/pill next to the existing status display showing the active mode:
      - "Pinch" badge when gestureMode === 'pinch': neutral background (rgba(255,255,255,0.15)), white text, small font (11px), borderRadius 4, padding '2px 6px'
      - "Wave" badge when gestureMode === 'wave': blue background (#2563EB), white text, same sizing
      - Badge appears only when `handTrackingReady === true` (no point showing mode until tracking is initialized)
    - Keep the badge visually compact — it is secondary information. Place it inline with or below the hand detection status text.

    In App.tsx:
    - Audit the info toast added in Plan 02 for z-index correctness (must be z:20, above toolbar at z:10)
    - Verify the toast auto-clears after exactly 3000ms and does not persist on re-renders
    - No other UX changes needed unless a visual rough edge is found during the read — if found, note it in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/programming/ar-project && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles; HandStatusIndicator shows mode badge when tracking is ready; badge is blue for Wave mode and neutral for Pinch mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 4 feature set: wave mode gesture logic, mode toggle button, Inspect/Wave exclusivity, gesture mode badge in status indicator.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` and open http://localhost:1420 (or Tauri: `pnpm tauri dev`).

    **Checklist — verify each item before approving:**

    1. Toolbar shows all expected buttons including the Wave Mode toggle at the right end.
    2. Click "Wave Mode" button — it turns blue; label changes to "Pinch Mode" (indicating next click switches back). Status indicator shows "Wave" badge.
    3. With Wave mode active, show open hand to webcam — model should rotate as you move your hand.
    4. Spread fingers wide — model zooms in continuously while held. Release — zoom stops.
    5. Make a fist — model zooms out continuously. Release — zoom stops.
    6. Make a pinch gesture while in Wave mode — model should NOT rotate or scale (pinch disabled in wave mode).
    7. Point your index finger — body part hover selection still works (label appears on dwell).
    8. Enable Inspect mode (click Inspect button) — Wave Mode button disappears from toolbar. If you were in Wave mode, a blue info toast appears: "Inspect mode activated. Switched to Pinch+Drag mode."
    9. Disable Inspect mode — Wave Mode toggle reappears.
    10. Bundle size check: run `pnpm build` and confirm gzip size in terminal output stays under 500KB for the main JS chunk.
  </how-to-verify>
  <resume-signal>Type "approved" if all items pass, or describe which items failed with specific behavior observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser render pipeline | All rendering is client-side; no new data inputs in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | gestureMode displayed in UI | accept | Mode display is intentional UX — no sensitive data exposed. |
| T-04-08 | Denial of Service | Bundle size bloat from polish additions | accept | Badge and toast are trivial DOM; no new dependencies added. Build output verified in human checkpoint. |
</threat_model>

<verification>
After Task 1:
- `pnpm tsc --noEmit` passes
- `grep -n "gestureMode" src/components/HandStatusIndicator.tsx` shows subscription and badge render

After human checkpoint approval:
- Both gesture modes function correctly end-to-end
- Mode exclusivity works (Inspect disables Wave)
- Bundle is under 500KB gzipped
- No console errors during gesture interaction
</verification>

<success_criteria>
- HandStatusIndicator shows "Wave" (blue) or "Pinch" (neutral) badge when hand tracking is ready
- All Phase 4 success criteria verified by human: rotation, zoom-in, zoom-out, mode toggle, conflict resolution
- `pnpm build` gzip size under 500KB for main JS chunk
- App runs without console errors during active gesture use
</success_criteria>

<output>
After completion, create `.planning/phases/04-second-gesture-mode-polish/04-04-SUMMARY.md`
</output>
