---
phase: 04-second-gesture-mode-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/hooks/useGestureInterpreter.ts
  - src/components/SceneController.tsx
autonomous: true
requirements:
  - GEST-03
  - GEST-04

must_haves:
  truths:
    - "In wave mode, open-hand movement (non-pinch, non-pointing) drives trackball rotation via hand center position (D-05 to D-07)"
    - "In wave mode, spread gesture drives continuous zoom-in; fist drives continuous zoom-out (D-09 to D-12)"
    - "Two-hand spread zoom: distance between index fingertips of left and right hands maps to zoom level (D-11)"
    - "In wave mode, pinch+drag is completely disabled — pinch is ignored (D-14)"
    - "Pointing gesture works in both modes for body part selection (D-15)"
    - "Inspect mode spread/fist for explode control is gated by inspectMode flag, unchanged (D-16)"
    - "Wave mode rotation uses the same rotationSensitivity Leva constant as pinch mode (D-08)"
    - "Wave mode zoom accumulates continuously while gesture is held (D-12)"
  artifacts:
    - path: "src/hooks/useGestureInterpreter.ts"
      provides: "Wave mode gesture branch (rotation, spread/fist zoom, two-hand spread)"
      contains: "wave-rotate"
    - path: "src/components/SceneController.tsx"
      provides: "SceneController handles wave-zoom GestureCommand"
      contains: "wave-zoom"
  key_links:
    - from: "src/hooks/useGestureInterpreter.ts"
      to: "src/store/appState.ts"
      via: "useAppStore.getState().gestureMode"
      pattern: "gestureMode.*wave"
    - from: "src/components/SceneController.tsx"
      to: "src/hooks/useGestureInterpreter.ts"
      via: "gestureCommandRef.current.type === 'wave-zoom'"
      pattern: "wave-zoom"
---

<objective>
Implement wave mode gesture logic inside useGestureInterpreter and add wave-zoom command handling in SceneController.

Purpose: This is the core feature delivery for GEST-03 — the open hand wave interaction mode. All wave mode behaviors (rotation from hand movement, zoom from spread/fist, two-hand spread zoom, mode isolation) are implemented here.
Output: useGestureInterpreter emits `rotate` commands from hand position in wave mode and `wave-zoom` commands from spread/fist; SceneController applies wave-zoom to model scale.
</objective>

<execution_context>
@/Users/paul/Documents/programming/ar-project/.planning/phases/04-second-gesture-mode-polish/04-CONTEXT.md
</execution_context>

<context>
@/Users/paul/Documents/programming/ar-project/.planning/ROADMAP.md

<interfaces>
<!-- From src/hooks/useGestureInterpreter.ts — existing structure to branch from -->
```typescript
// Key refs available inside interpret():
// gestureStateRef: GestureState { mode: GestureMode; pinchOrigin: { x: number; y: number } | null }
// isPinchingRef, prevMidpointRef, gestureOffTimerRef, pointingNDCRef, smoothNDCRef

// Key Leva constants available:
// PINCH_ENTER, PINCH_EXIT, DEAD_ZONE_PX, rotationSensitivity, scaleSensitivity

// Store access:
// useAppStore.getState().gestureMode  — 'pinch' | 'wave'
// useAppStore.getState().inspectMode  — boolean
// setGestureActive(true/false)
// setExplodeActive(true/false)

// Gesture helper functions (module-level, already exist):
// isPointing(hand), isSpread(hand), isFist(hand)
// fingerExtended(hand, tip, pip, mcp), fingerCurled(hand, tip, pip, mcp)
// dist(a, b)  — Euclidean distance between two NormalizedLandmarks
```

<!-- From src/types/gestures.ts — AFTER Plan 01 is applied -->
```typescript
export type GestureMode = 'idle' | 'pinching' | 'two-hand-pinch' | 'wave-rotate' | 'wave-zoom';
export type GestureCommand =
  | { type: 'idle' }
  | { type: 'rotate'; delta: { x: number; y: number } }
  | { type: 'scale'; factor: number }
  | { type: 'pan'; delta: { x: number; y: number } }
  | { type: 'wave-zoom'; direction: 'in' | 'out'; speed: number };
```

<!-- From src/components/SceneController.tsx — existing command handling pattern -->
<!-- SceneController reads gestureCommandRef.current each frame in useFrame -->
<!-- Existing handled types: 'rotate' (applies to modelGroupRef Euler), 'scale' (multiplies scale), 'pan' (moves position) -->
<!-- wave-zoom must multiply modelGroupRef.current.scale.multiplyScalar(factor) each frame the command is active -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement wave mode branch in useGestureInterpreter</name>
  <files>src/hooks/useGestureInterpreter.ts</files>
  <read_first>
    - src/hooks/useGestureInterpreter.ts — full file; understand the interpret() function structure, ref usage, and where the early-exit branches are
    - src/types/gestures.ts — confirm GestureMode and GestureCommand types after Plan 01
    - src/store/appState.ts — confirm gestureMode field exists after Plan 01
  </read_first>
  <behavior>
    - When gestureMode='wave' and hand is pointing: pointingNDC is computed (same as pinch mode); no rotation or zoom emitted
    - When gestureMode='wave' and hand is spread (all 4 non-thumb fingers extended): emit wave-zoom 'in' with speed proportional to average inter-fingertip distance
    - When gestureMode='wave' and hand is fist (all 4 non-thumb fingers curled): emit wave-zoom 'out' with speed proportional to average inter-fingertip distance when spread was last measured (use constant 0.5 as fallback)
    - When gestureMode='wave' and hand is open (not pointing, not spread, not fist): emit rotate using hand center (landmark 9) position delta, same trackball logic as pinch rotate, using rotationSensitivity
    - When gestureMode='wave' and landmarks.length >= 2 with spread on both: use index fingertip distance (hand0[8] to hand1[8]) as zoom speed signal
    - When gestureMode='pinch': existing logic is entirely unchanged
  </behavior>
  <action>
    Inside the `interpret` callback, immediately after reading `inspectMode` from the store, read `gestureMode`:
    `const gestureMode = useAppStore.getState().gestureMode;`

    Add a new early-return branch for wave mode BEFORE the existing pinch detection block. The wave mode branch must:

    1. Pointing passthrough (D-15): If `isPointing(hand0)` is true, compute pointingNDCRef (same EMA logic as existing shouldRaycast block — extract this to avoid duplication, or call the same block). Return `{ type: 'idle' }`.

    2. Two-hand spread zoom (D-11): If `landmarks.length >= 2` and `isSpread(hand0)` and `isSpread(landmarks[1])`:
       - Compute distance between `hand0[8]` (index fingertip) and `landmarks[1][8]` (other hand index fingertip) using the `dist()` helper
       - Normalize: `speed = Math.min(dist(...) * 2, 1.0)` (two hands spread far apart = speed 1.0)
       - Set gestureStateRef to `{ mode: 'wave-zoom', pinchOrigin: null }`
       - Call `setGestureActive(true)`
       - Return `{ type: 'wave-zoom', direction: 'in', speed }`

    3. Single-hand spread zoom in (D-09): If `isSpread(hand0)`:
       - Compute average distance between adjacent fingertips: `(dist(hand0[8],hand0[12]) + dist(hand0[12],hand0[16]) + dist(hand0[16],hand0[20])) / 3`
       - Normalize to 0–1 range: `speed = Math.min(avgDist * 4, 1.0)` (empirical multiplier; tunable)
       - Set gestureStateRef to `{ mode: 'wave-zoom', pinchOrigin: null }`
       - Call `setGestureActive(true)`
       - Return `{ type: 'wave-zoom', direction: 'in', speed }`

    4. Fist zoom out (D-10): If `isFist(hand0)`:
       - Speed: use constant `0.5` (fist has no spread distance to measure; constant speed keeps it predictable)
       - Set gestureStateRef to `{ mode: 'wave-zoom', pinchOrigin: null }`
       - Call `setGestureActive(true)`
       - Return `{ type: 'wave-zoom', direction: 'out', speed: 0.5 }`

    5. Open-hand rotation (D-05 to D-07): If none of the above match (hand is open but not pointing/spread/fist):
       - Use landmark 9 (middle finger MCP) as hand center, same as pinch rotate
       - `const handCenter = { x: hand0[9].x * videoWidth, y: hand0[9].y * videoHeight }`
       - If `gestureStateRef.current.mode !== 'wave-rotate'`: store handCenter as origin, set mode 'wave-rotate', call `setGestureActive(true)`, return `{ type: 'idle' }` (first frame skip)
       - Else: compute delta from stored origin, apply dead zone (DEAD_ZONE_PX), update origin (incremental trackball), return `{ type: 'rotate', delta: { x: delta.x * rotationSensitivity, y: delta.y * rotationSensitivity } }`

    6. Wave mode idle exit: If wave mode is active but no matching gesture, reset state refs and return `{ type: 'idle' }`.

    The wave mode branch must NOT fall through to the pinch detection block. Place an explicit `return { type: 'idle' }` at the bottom of the wave block to prevent accidental fall-through.

    The existing `inspectMode` spread/fist explode block (lines 160–163) must remain OUTSIDE the wave mode branch and only run when `inspectMode === true`, regardless of gestureMode. This preserves D-16 (inspect mode is its own system).

    Add `gestureMode` to the useCallback dependency array.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/programming/ar-project && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles; wave mode branch exists in interpret(); gestureMode is in useCallback deps; existing pinch block is unreachable when gestureMode='wave'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handle wave-zoom command in SceneController</name>
  <files>src/components/SceneController.tsx</files>
  <read_first>
    - src/components/SceneController.tsx — full file; find the useFrame loop and the switch/if block that dispatches on gestureCommandRef.current.type
    - src/types/gestures.ts — confirm wave-zoom variant shape: { type: 'wave-zoom'; direction: 'in' | 'out'; speed: number }
  </read_first>
  <behavior>
    - wave-zoom 'in' with speed 0.5 increases model scale by ~1% per frame at 60fps
    - wave-zoom 'out' with speed 0.5 decreases model scale by ~1% per frame at 60fps
    - wave-zoom 'in' with speed 1.0 increases model scale by ~2% per frame (max speed)
    - Model scale is clamped between 0.1 (min) and 5.0 (max) to prevent infinite zoom (D per 04-CONTEXT specifics)
    - Scale is applied to modelGroupRef.current.scale uniformly (x, y, z all same factor)
  </behavior>
  <action>
    In SceneController's useFrame handler, in the block that reads `gestureCommandRef.current`, add a new case for `wave-zoom`:

    - If `cmd.type === 'wave-zoom'`:
      - Compute `zoomSpeed = cmd.speed * 0.02` — maps speed 0–1 to scale delta 0–0.02 per frame
      - If `cmd.direction === 'in'`: `factor = 1 + zoomSpeed`
      - If `cmd.direction === 'out'`: `factor = 1 - zoomSpeed`
      - Get current uniform scale: `const currentScale = modelGroupRef.current.scale.x`
      - Clamp result: `const newScale = Math.max(0.1, Math.min(5.0, currentScale * factor))`
      - Apply: `modelGroupRef.current.scale.setScalar(newScale)`

    The clamping (0.1 to 5.0) prevents runaway zoom that would break the demo experience (D per 04-CONTEXT specifics section).

    Do not modify handling for 'rotate', 'scale', or 'pan' command types. Add the wave-zoom case without disrupting existing flow.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/programming/ar-project && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles; SceneController handles wave-zoom command; scale clamp logic present; existing rotate/scale/pan handling unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaPipe landmarks → gesture interpreter | Landmark values are normalized 0–1 floats from MediaPipe WASM; no user-controlled string input |
| Gesture command → SceneController | Internal ref pass, no serialization or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Wave zoom infinite scale | mitigate | Scale clamped to 0.1–5.0 in SceneController wave-zoom handler. |
| T-04-05 | Denial of Service | High-frequency gesture frames CPU cost | accept | MediaPipe already throttled to 30fps (Phase 2 setting); gesture interpreter runs inside that loop. No additional cost from wave branch. |
| T-04-06 | Spoofing | Wave mode active in pinch mode | mitigate | Wave branch gated by `gestureMode === 'wave'` store check each frame; cannot be bypassed via UI (button is the only toggle). |
</threat_model>

<verification>
After both tasks:
- `pnpm tsc --noEmit` passes
- `grep -n "wave-zoom\|wave-rotate\|gestureMode" src/hooks/useGestureInterpreter.ts` shows all three
- `grep -n "wave-zoom" src/components/SceneController.tsx` shows the handler
- `grep -n "setScalar\|0\.1\|5\.0" src/components/SceneController.tsx` shows the clamped scale logic
- Manual test: switch to Wave mode in toolbar, hold hand open in front of webcam, model should rotate; spread fingers, model should zoom in; make fist, model should zoom out
</verification>

<success_criteria>
- Open hand movement in wave mode rotates model via trackball (same sensitivity as pinch mode)
- Spread gesture zooms in continuously; fist gesture zooms out continuously
- Two-hand spread (both hands spread) also triggers zoom-in at higher speed
- Pinch gesture in wave mode is completely ignored (no rotation/scale emitted)
- Pointing gesture works in wave mode for body part selection
- Scale is clamped at 0.1x min and 5.0x max
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-second-gesture-mode-polish/04-03-SUMMARY.md`
</output>
