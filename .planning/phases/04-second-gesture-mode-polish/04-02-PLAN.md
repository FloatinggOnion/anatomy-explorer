---
phase: 04-second-gesture-mode-polish
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/components/BottomToolbar.tsx
  - src/App.tsx
autonomous: true
requirements:
  - GEST-04

must_haves:
  truths:
    - "Wave Mode toggle button is visible in the bottom toolbar"
    - "Active mode button is highlighted (blue accent fill); inactive is ghost style"
    - "Wave Mode button is hidden when inspectMode is true (D-16)"
    - "Switching to Wave mode while inspectMode is active auto-disables inspectMode and shows toast (D-16)"
    - "Clicking Wave Mode button sets gestureMode to 'wave'; clicking Pinch Mode sets it to 'pinch' (D-04)"
  artifacts:
    - path: "src/components/BottomToolbar.tsx"
      provides: "Gesture mode toggle button with active state styling"
      contains: "Wave Mode"
    - path: "src/App.tsx"
      provides: "Inspect/Wave mutual exclusivity side-effect logic"
      contains: "gestureMode"
  key_links:
    - from: "src/components/BottomToolbar.tsx"
      to: "src/store/appState.ts"
      via: "useAppStore((s) => s.gestureMode) and setGestureMode"
      pattern: "gestureMode"
    - from: "src/App.tsx"
      to: "src/store/appState.ts"
      via: "useEffect watching inspectMode and gestureMode"
      pattern: "setGestureMode.*pinch|setInspectMode.*false"
---

<objective>
Add the gesture mode toggle button to BottomToolbar with full active state styling, and wire the Inspect ↔ Wave mode mutual exclusivity rule in App.tsx.

Purpose: Delivers GEST-04 (user-visible mode toggle) and the D-16 conflict resolution. The toolbar is the user's only mechanism to switch gesture modes — it must clearly show which mode is active.
Output: BottomToolbar with Wave/Pinch toggle buttons; App.tsx useEffect for Inspect/Wave exclusivity with toast.
</objective>

<execution_context>
@/Users/paul/Documents/programming/ar-project/.planning/phases/04-second-gesture-mode-polish/04-CONTEXT.md
</execution_context>

<context>
@/Users/paul/Documents/programming/ar-project/.planning/ROADMAP.md

<interfaces>
<!-- From src/store/appState.ts — AFTER Plan 01 is applied -->
```typescript
gestureMode: 'pinch' | 'wave';
setGestureMode: (mode: 'pinch' | 'wave') => void;
inspectMode: boolean;
setInspectMode: (v: boolean) => void;
// All existing Phase 2–3 fields unchanged
```

<!-- From src/components/BottomToolbar.tsx — existing button pattern to follow -->
<!-- Active button style: background '#2563EB', border 'none', color '#ffffff' -->
<!-- Ghost button style: background 'transparent', border '1px solid rgba(255,255,255,0.3)', color '#ffffff' -->
<!-- Transition for mode button: 'background 200ms, border 200ms, box-shadow 200ms' (D per 04-CONTEXT specifics) -->

<!-- From src/App.tsx — existing imports and AppInner structure -->
<!-- setHandDetected, interpret, videoRef, gestureCommandRef, pointingNDCRef are all wired -->
<!-- useAppStore is already imported -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gesture mode toggle button to BottomToolbar</name>
  <files>src/components/BottomToolbar.tsx</files>
  <read_first>
    - src/components/BottomToolbar.tsx — full file; understand existing button styles, Divider pattern, state subscriptions
    - src/store/appState.ts — confirm gestureMode field and setGestureMode exist after Plan 01
  </read_first>
  <action>
    Add to the useAppStore subscriptions at the top of BottomToolbar:
    - `const gestureMode = useAppStore((s) => s.gestureMode);`
    - `const setGestureMode = useAppStore((s) => s.setGestureMode);`

    After the Inspect button group (item 9 currently), add:
    - A Divider (item 10)
    - A "Wave Mode" button (item 11) with the following behavior:
      - When `gestureMode === 'wave'`: active style (background '#2563EB', border 'none')
      - When `gestureMode === 'pinch'`: ghost style (background 'transparent', border '1px solid rgba(255,255,255,0.3)')
      - `style.transition`: 'background 200ms, border 200ms' for smooth toggle feedback (D-02)
      - onClick: `setGestureMode(gestureMode === 'wave' ? 'pinch' : 'wave')` — toggle between modes
      - Conditional rendering: only render this button when `!inspectMode` (D-16: wave button hidden while inspect is active)
      - Label: "Wave Mode" when `gestureMode === 'pinch'`; "Pinch Mode" when `gestureMode === 'wave'` — shows which mode you switch TO for clarity

    Do not modify existing buttons. Keep the Divider before the wave button inside the same conditional so no orphan divider appears when inspect mode hides the button.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/programming/ar-project && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles; BottomToolbar renders Wave Mode toggle button; button is absent when inspectMode is true; active state shows blue background; button label alternates between "Wave Mode" and "Pinch Mode".</done>
</task>

<task type="auto">
  <name>Task 2: Wire Inspect ↔ Wave mutual exclusivity in AppInner</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx — full file; understand AppInner's existing useAppStore subscriptions, useEffect patterns
    - src/store/appState.ts — confirm setGestureMode and setInspectMode signatures
  </read_first>
  <action>
    In AppInner, add two useEffect hooks for mutual exclusivity (D-16):

    Effect 1 — When inspectMode activates while gestureMode is 'wave':
    - Subscribe to `inspectMode` from useAppStore
    - When `inspectMode` transitions to `true`, if `gestureMode === 'wave'`:
      - Call `setGestureMode('pinch')` to switch back to pinch mode
      - Set a local toast message state: "Inspect mode activated. Switched to Pinch+Drag mode."
    - Implement toast via a local useState<string | null> and a useEffect that auto-clears it after 3000ms
    - Render the toast as a fixed div (bottom-right, z:20) with the same error toast styling already in BottomToolbar (background '#1e3a5f', color '#93c5fd', padding '12px 16px', borderRadius 8, fontSize 14) — but use a blue info color (not red) to distinguish from error toasts
    - Do not set `inspectMode` to false from this effect — toolbar already handles Inspect button toggling

    Effect 2 — When gestureMode switches to 'wave' while inspectMode is true:
    - This case is prevented at the UI level (wave button hidden when inspectMode is true), so no additional effect is needed
    - Add a comment noting this: "Wave button is hidden in BottomToolbar when inspectMode=true, so this case cannot be triggered via normal UI"

    Import `useState` if not already imported (it is — check existing imports first).
    Access `gestureMode` and `setGestureMode` from `useAppStore` inside AppInner.
    Access `inspectMode` from `useAppStore` inside AppInner (already set by BottomToolbar, just read here).
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/programming/ar-project && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles; when user enables Inspect mode while in Wave mode, gestureMode resets to 'pinch' and a blue info toast appears for 3 seconds; toast does not appear when switching from Pinch to Inspect mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI buttons | All input is explicit button presses — no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | Mode toggle rapid clicking | accept | React state batches rapid clicks; gestureMode is simple string, no debounce needed. Worst case: gestureMode flickers between 'pinch' and 'wave' — harmless. |
| T-04-03 | Elevation of Privilege | Inspect + Wave active simultaneously | mitigate | BottomToolbar hides wave button when inspectMode=true; App.tsx effect auto-resets gestureMode to 'pinch' if inspectMode enables while in wave. Double-gated. |
</threat_model>

<verification>
After both tasks:
- `pnpm tsc --noEmit` passes
- `grep -n "Wave Mode\|gestureMode" src/components/BottomToolbar.tsx` shows button and store subscription
- `grep -n "inspectMode.*gestureMode\|gestureMode.*pinch" src/App.tsx` shows the exclusivity effect
- Visually: run `pnpm dev`, open browser, confirm Wave Mode button visible in toolbar; click it, confirm it highlights blue; enable Inspect mode, confirm Wave Mode button disappears
</verification>

<success_criteria>
- Wave Mode toggle button renders in toolbar; clicking it toggles gestureMode between 'pinch' and 'wave'
- Active mode button has blue (#2563EB) background; inactive has ghost border
- Wave Mode button is hidden when inspectMode is true
- Enabling Inspect mode while in Wave mode auto-resets gestureMode to 'pinch' and shows info toast
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-second-gesture-mode-polish/04-02-SUMMARY.md`
</output>
