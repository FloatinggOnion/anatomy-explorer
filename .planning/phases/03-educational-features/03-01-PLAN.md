---
phase: 03-educational-features
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/appState.ts
  - src/components/ModelGalleryDrawer.tsx
  - src/components/BottomToolbar.tsx
  - public/models/skeleton-thumb.png
  - public/models/body-thumb.png
  - scripts/generate-thumbnails.mjs
autonomous: true
requirements:
  - MDL-02

must_haves:
  truths:
    - "User can tap 'Models' button in the bottom toolbar to open a gallery drawer"
    - "Drawer shows 'Skeleton' and 'Body' entries with thumbnails and closes on model selection"
    - "Selecting a model from the drawer loads it and replaces the current model"
    - "Active model is visually highlighted with a blue left border in the drawer"
    - "'+ Load file' entry in the drawer opens a file picker and moves file-loading out of the toolbar"
    - "The old 'Load Model' button no longer appears in the toolbar"
  artifacts:
    - path: "src/store/appState.ts"
      provides: "Phase 3 Zustand store fields (drawerOpen, inspectMode, explodeActive, visibleLayers, availableLayers, selectedMeshName)"
      contains: "drawerOpen"
    - path: "src/components/ModelGalleryDrawer.tsx"
      provides: "Gallery drawer component with model list, thumbnails, and file picker"
      exports: ["ModelGalleryDrawer"]
    - path: "src/components/BottomToolbar.tsx"
      provides: "Updated toolbar with Models button, Layers/Explode/Inspect stubs, no Load Model button"
      contains: "Models"
    - path: "public/models/skeleton-thumb.png"
      provides: "Thumbnail for procedural skeleton entry"
    - path: "public/models/body-thumb.png"
      provides: "Thumbnail for body.glb entry"
  key_links:
    - from: "src/components/BottomToolbar.tsx"
      to: "src/store/appState.ts"
      via: "setDrawerOpen(true) on Models button click"
      pattern: "setDrawerOpen"
    - from: "src/components/ModelGalleryDrawer.tsx"
      to: "src/store/appState.ts"
      via: "setModelUrl on item selection; drawerOpen gate"
      pattern: "setModelUrl|setDrawerOpen"
    - from: "src/App.tsx"
      to: "src/components/ModelGalleryDrawer.tsx"
      via: "Rendered at z:10 overlay layer in AppInner"
      pattern: "ModelGalleryDrawer"
---

## Phase Goal

**As a** anatomy student, **I want to** browse and select different anatomy models from a gallery, **so that** I can switch between the skeleton, body model, and my own files without losing the AR canvas.

<objective>
Deliver the model gallery slice: extend the Zustand store with all Phase 3 fields, build the ModelGalleryDrawer component, and restructure BottomToolbar (Models button replaces Load Model; stub Layers/Explode/Inspect buttons added but inactive).

Purpose: Enables model browsing (MDL-02) as a complete vertical feature — from button tap to model load — while setting up store state that Plans 02 and 03 depend on.
Output: ModelGalleryDrawer.tsx (new), appState.ts (extended), BottomToolbar.tsx (restructured), two thumbnail PNG files, App.tsx (add drawer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-educational-features/03-CONTEXT.md
@.planning/phases/03-educational-features/03-UI-SPEC.md
@.planning/phases/03-educational-features/03-PATTERNS.md

<interfaces>
From src/store/appState.ts (current — extend this, do not replace):
```typescript
export const useAppStore = create<AppState>((set) => ({
  // Phase 1 + Phase 2 fields already present — keep all
  modelUrl: null,
  setModelUrl: (url) => set({ modelUrl: url }),
  // ... (read the file before editing)
}));
```

From src/components/BottomToolbar.tsx (current shape to replace):
- Has: Load Model button, hidden file input, Landmarks toggle
- Phase 3: Remove Load Model button + file input; add Models, Layers (stub), Explode (stub), Inspect (stub) buttons
- Move CR-04 object URL handling into ModelGalleryDrawer.tsx

From src/App.tsx (rendered at z:10, add ModelGalleryDrawer alongside HandStatusIndicator):
```typescript
// Current z:10 block:
<HandStatusIndicator />
<BottomToolbar />
// Add:
<ModelGalleryDrawer />
```

UI-SPEC button order (left to right in toolbar):
1. "Models" — accent fill (#2563EB bg, white text), opens drawer
2. Divider
3. "Landmarks ON/OFF" — ghost style (unchanged)
4. Divider
5. "Layers" — ghost style, disabled (opacity 0.4, cursor not-allowed) in Plan 01
6. Divider
7. "Explode" — ghost style, disabled in Plan 01
8. Divider
9. "Inspect" — ghost style, disabled in Plan 01
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Zustand store with all Phase 3 fields</name>
  <files>src/store/appState.ts</files>
  <read_first>
    - src/store/appState.ts — MUST read before editing; append Phase 3 block after Phase 2 block, preserving all existing fields
    - .planning/phases/03-educational-features/03-PATTERNS.md — "src/store/appState.ts (store extension)" section for exact field names and setter pattern
  </read_first>
  <action>
    Append a "Phase 3 fields" block to the AppState interface and the create() implementation.

    Add to the interface after the Phase 2 fields:
    - drawerOpen: boolean; setDrawerOpen: (v: boolean) => void
    - inspectMode: boolean; setInspectMode: (v: boolean) => void
    - explodeActive: boolean; setExplodeActive: (v: boolean) => void
    - visibleLayers: Set&lt;string&gt;; setVisibleLayers: (v: Set&lt;string&gt;) => void
    - availableLayers: string[]; setAvailableLayers: (names: string[]) => void
    - selectedMeshName: string | null; setSelectedMeshName: (name: string | null) => void

    Add to the create() implementation block (after Phase 2 entries, same setter pattern (v) => set({ field: v })):
    - drawerOpen: false
    - inspectMode: false
    - explodeActive: false
    - visibleLayers: new Set&lt;string&gt;()
    - availableLayers: []
    - selectedMeshName: null

    Naming: use exactly these identifiers — downstream plans reference them by name.
  </action>
  <verify>
    <automated>grep -c "drawerOpen\|inspectMode\|explodeActive\|visibleLayers\|availableLayers\|selectedMeshName" src/store/appState.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep count from verify command is 12 (6 interface declarations + 6 implementation values)
    - File compiles: `pnpm exec tsc --noEmit` exits 0
    - All Phase 1 and Phase 2 fields still present (modelUrl, gestureActive, landmarksVisible, handDetected, handTrackingReady, modelLoadError, handTrackingError)
  </acceptance_criteria>
  <done>appState.ts exports useAppStore with all 6 Phase 3 fields typed and initialised; TypeScript compilation succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create thumbnail PNGs and ModelGalleryDrawer component</name>
  <files>
    scripts/generate-thumbnails.mjs,
    public/models/skeleton-thumb.png,
    public/models/body-thumb.png,
    src/components/ModelGalleryDrawer.tsx
  </files>
  <read_first>
    - .planning/phases/03-educational-features/03-UI-SPEC.md — "Surface 1: Model Gallery Drawer" section for exact dimensions, colors, layout, and interaction behavior
    - .planning/phases/03-educational-features/03-PATTERNS.md — "src/components/ModelGalleryDrawer.tsx" section for BottomToolbar analog patterns (file input lifecycle, z:10 positioning, active highlight pattern)
    - src/components/BottomToolbar.tsx — CR-04 object URL lifecycle pattern (lines 23–53) to replicate in the drawer
    - src/store/appState.ts — after Task 1 completes, confirm store field names before use
  </read_first>
  <action>
    Step A — Generate thumbnail PNGs.
    Create scripts/generate-thumbnails.mjs as a Node ESM script that writes two minimal valid PNG files:
    - public/models/skeleton-thumb.png: 64x64, solid #e8dcc8 (ivory — matches skeleton BoneMesh color)
    - public/models/body-thumb.png: 64x64, solid #a8c4d4 (muted blue — body model placeholder)
    A minimal valid PNG can be produced using the `zlib` and `crypto` built-in modules to write PNG chunks (IHDR, IDAT with a flat color row, IEND). Run `node scripts/generate-thumbnails.mjs` after writing to produce the files.

    Step B — Build ModelGalleryDrawer.tsx.
    The component must:
    - Subscribe to drawerOpen (gate render) and modelUrl (active highlight) from useAppStore via individual selectors
    - Maintain a local prevObjectUrlRef for CR-04 URL lifecycle management (same as BottomToolbar had)
    - Define a MODELS constant array at module top with entries: skeleton (url: null, label: 'Skeleton', thumb: '/models/skeleton-thumb.png') and body (url: '/models/body.glb', label: 'Body', thumb: '/models/body-thumb.png')
    - Handle selecting a bundled model: call setModelUrl(entry.url), setAvailableLayers([]), setVisibleLayers(new Set()), setExplodeActive(false), setSelectedMeshName(null), setDrawerOpen(false) — per D-04 and D-13 resets
    - Handle '+ Load file' click: trigger a hidden file input ref; on file change use the CR-04 pattern from BottomToolbar (URL.createObjectURL, useGLTF.clear(prev), URL.revokeObjectURL(prev), setModelUrl(newUrl)), then call resetPhase3State() and setDrawerOpen(false)
    - Reset layer/explode/selection state on model change (D-13): extracted as a helper resetPhase3State() calling setAvailableLayers([]), setVisibleLayers(new Set()), setExplodeActive(false), setSelectedMeshName(null)
    - Render: when drawerOpen is false, render null immediately
    - When open: render a fixed backdrop (rgba(0,0,0,0.4), inset 0, z:10) that closes drawer on click, plus the drawer panel (position fixed, top 0, left 0, bottom 0, width 280, zIndex 10, transform translateX(0 when open), transition 0.25s ease-out, background rgba(17,24,39,0.92), backdropFilter blur(8px))
    - Drawer header: "Select Model" at 16px/600/#ffffff, padding 16px, bottom border rgba(255,255,255,0.1) 1px solid
    - Model list items: padding 12px 16px, display flex, gap 12px, align-items center; thumbnail is an img 56x56 borderRadius 6 objectFit cover; model name 14px/400/#ffffff
    - Active item (entry.url === modelUrl for GLBs, or entry.url === null when modelUrl === null for skeleton): left border 3px solid #2563EB, background rgba(37,99,235,0.15)
    - Hover state on list items: background rgba(255,255,255,0.08) — use onMouseEnter/onMouseLeave local state or CSS
    - Separator and '+ Load file' entry below the model list per UI-SPEC (separator rgba(255,255,255,0.1) 1px margin 8px top/bottom; text 14px/400/rgba(255,255,255,0.7))
    - useEffect cleanup: revoke prevObjectUrlRef on unmount (CR-01 pattern)
    - Accessibility: all interactive elements have cursor: pointer; drawer backdrop has aria-label="Close model gallery"

    Import useGLTF from '@react-three/drei' for cache clearing (same as BottomToolbar).
  </action>
  <verify>
    <automated>node scripts/generate-thumbnails.mjs && ls public/models/skeleton-thumb.png public/models/body-thumb.png && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - public/models/skeleton-thumb.png and public/models/body-thumb.png both exist and are valid PNG files (file command shows "PNG image data")
    - ModelGalleryDrawer.tsx exports a named function component `ModelGalleryDrawer`
    - grep "drawerOpen" src/components/ModelGalleryDrawer.tsx exits 0 with matches
    - grep "CR-04\|useGLTF.clear" src/components/ModelGalleryDrawer.tsx exits 0 with matches (CR-04 pattern present)
    - grep "resetPhase3State\|setAvailableLayers\|setDrawerOpen" src/components/ModelGalleryDrawer.tsx exits 0 with matches
    - TypeScript: pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Thumbnail PNGs exist; ModelGalleryDrawer renders the correct drawer UI; opening/selecting/closing works; file loading moved from toolbar into drawer with CR-04 lifecycle intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restructure BottomToolbar and wire drawer into App.tsx</name>
  <files>
    src/components/BottomToolbar.tsx,
    src/App.tsx
  </files>
  <read_first>
    - src/components/BottomToolbar.tsx — read full current file before editing; remove Load Model button + file input + CR-04 handling (moved to drawer in Task 2)
    - src/App.tsx — read full current file; add ModelGalleryDrawer import and render it at z:10 layer
    - .planning/phases/03-educational-features/03-UI-SPEC.md — "Surface 4: Toolbar Additions" section for exact button order and styles
    - .planning/phases/03-educational-features/03-PATTERNS.md — "src/components/BottomToolbar.tsx" section for button style constants
  </read_first>
  <action>
    BottomToolbar.tsx changes:
    - Remove: fileInputRef, prevObjectUrlRef, handleFileChange, the 2s loading spinner useEffect, the URL revoke useEffect, the CR-04 file input element, and the "Load Model" button entirely
    - Keep: the error toast block, the loading spinner overlay, isLoading state (loading spinner is still used; the modelLoadError useEffect for auto-dismiss stays)
    - Add selectors from useAppStore: setDrawerOpen, availableLayers, explodeActive, setExplodeActive, inspectMode, setInspectMode (do NOT add setLayersOpen — use local useState for chip row expand toggle in Plan 03; stub the Layers button with disabled state for now)
    - New button order per UI-SPEC Surface 4:
      1. "Models" button — accent fill (#2563EB bg, border: none, color: #fff, height 36, fontSize 12, fontWeight 400, borderRadius 6, padding 0 12px, cursor pointer) → onClick: setDrawerOpen(true)
      2. Divider (width 1, height 24, background rgba(255,255,255,0.2))
      3. "Landmarks ON/OFF" toggle — ghost style, unchanged from Phase 2
      4. Divider
      5. "Layers" button — ghost style, disabled: opacity 0.4, cursor not-allowed (stub; Plan 03 enables it). Add aria-disabled="true".
      6. Divider
      7. "Explode" toggle — active fill style when explodeActive, ghost style when not; onClick: setExplodeActive(!explodeActive)
      8. Divider
      9. "Inspect" toggle — active fill style when inspectMode, ghost style when not; onClick: setInspectMode(!inspectMode)
    - Active fill style: background #2563EB, border: none, color #fff
    - Ghost style: background transparent, border 1px solid rgba(255,255,255,0.3), color #fff
    - All button heights: 36px, borderRadius 6, fontSize 12, fontWeight 400, padding 0 12px

    App.tsx changes:
    - Add import: import { ModelGalleryDrawer } from '@/components/ModelGalleryDrawer'
    - In AppInner return, add `<ModelGalleryDrawer />` after `<HandStatusIndicator />` and before `<BottomToolbar />`

    Do not add LayerChipRow to BottomToolbar yet — that is Plan 03's task.
  </action>
  <verify>
    <automated>grep -v "Load Model\|fileInputRef\|handleFileChange" src/components/BottomToolbar.tsx | grep -c "Models\|Layers\|Explode\|Inspect"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Load Model" src/components/BottomToolbar.tsx returns no matches
    - grep "fileInputRef\|handleFileChange" src/components/BottomToolbar.tsx returns no matches
    - grep "setDrawerOpen" src/components/BottomToolbar.tsx returns a match
    - grep "ModelGalleryDrawer" src/App.tsx returns a match
    - grep "Explode\|Inspect\|Layers\|Models" src/components/BottomToolbar.tsx returns 4+ matches
    - pnpm exec tsc --noEmit exits 0
    - pnpm run build exits 0 (Vite build succeeds)
  </acceptance_criteria>
  <done>Toolbar shows Models, Landmarks, Layers (disabled), Explode, Inspect buttons; Load Model button is gone; ModelGalleryDrawer is rendered in App.tsx; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file input → GLB loader | User-supplied GLB files enter via file picker; handled by existing ModelErrorBoundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ModelGalleryDrawer file input | mitigate | CR-04 pattern: clear useGLTF cache and revoke previous object URL before setting new URL; ModelErrorBoundary catches parse errors |
| T-03-02 | Denial of Service | Object URL accumulation | mitigate | prevObjectUrlRef lifecycle: revoke previous on new load, revoke all on unmount (CR-01 pattern) |
</threat_model>

<verification>
After completing all three tasks:
1. `pnpm run build` exits 0
2. `pnpm exec tsc --noEmit` exits 0
3. App renders in browser: toolbar shows Models, Landmarks, Layers (disabled), Explode, Inspect buttons
4. Clicking "Models" opens the gallery drawer from the left edge
5. Clicking "Skeleton" or "Body" in the drawer loads the model and closes the drawer
6. Active model has blue left border highlight in drawer
7. "+ Load file" opens file picker; selecting a GLB loads it
8. Explode and Inspect buttons toggle their active (blue fill) / inactive (ghost) states
9. No TypeScript errors
</verification>

<success_criteria>
- MDL-02 model gallery drawer fully functional: open, browse, select, close
- File loading moved into drawer; Load Model button removed from toolbar
- All Phase 3 store fields exist and are typed correctly
- Thumbnail PNGs exist in public/models/
- Build succeeds with zero TS errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-educational-features/03-01-SUMMARY.md`
</output>
