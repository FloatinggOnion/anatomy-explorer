---
phase: 03-educational-features
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/components/ExplodeController.tsx
  - src/components/ModelViewer.tsx
  - src/components/LayerChipRow.tsx
  - src/components/BottomToolbar.tsx
  - src/hooks/useGestureInterpreter.ts
  - src/components/Canvas.tsx
autonomous: true
requirements:
  - MDL-04
  - MDL-05

must_haves:
  truths:
    - "User can toggle named mesh groups visible/hidden via layer chips that appear above the toolbar"
    - "Layer chips appear only when the loaded model has named groups; Layers button is disabled otherwise"
    - "User can click Explode to smoothly animate model parts outward from center over ~0.5 seconds"
    - "Explode button is disabled (no named groups) or functional (has named groups); toggle animates parts back on second click"
    - "User can enable Inspect mode and use spread-fingers gesture to trigger explode ON, fist to trigger explode OFF"
    - "Pinch+drag rotation still works normally in Inspect mode"
    - "All layer state resets when a new model loads"
  artifacts:
    - path: "src/components/ExplodeController.tsx"
      provides: "R3F invisible component: per-frame lerp to explode/rest positions + layer visibility effect"
      exports: ["ExplodeController"]
    - path: "src/components/LayerChipRow.tsx"
      provides: "Expandable chip row above toolbar with layer toggle chips"
      exports: ["LayerChipRow"]
    - path: "src/components/ModelViewer.tsx"
      provides: "Extended with onGroupsScanned callback prop; calls setAvailableLayers on load"
      exports: ["ModelViewer"]
    - path: "src/components/BottomToolbar.tsx"
      provides: "Layers button enabled/disabled based on availableLayers; LayerChipRow toggled"
      contains: "LayerChipRow"
    - path: "src/hooks/useGestureInterpreter.ts"
      provides: "Spread/fist gesture detection added; gates on inspectMode via getState()"
  key_links:
    - from: "src/components/ModelViewer.tsx"
      to: "src/store/appState.ts"
      via: "onGroupsScanned callback → setAvailableLayers + setVisibleLayers(new Set(names))"
      pattern: "setAvailableLayers"
    - from: "src/store/appState.ts"
      to: "src/components/ExplodeController.tsx"
      via: "explodeActive and availableLayers selectors read in ExplodeController"
      pattern: "explodeActive"
    - from: "src/store/appState.ts"
      to: "src/hooks/useGestureInterpreter.ts"
      via: "useAppStore.getState().inspectMode — non-reactive read inside interpret()"
      pattern: "getState"
    - from: "src/components/BottomToolbar.tsx"
      to: "src/components/LayerChipRow.tsx"
      via: "Rendered inline above toolbar when layersOpen local state is true"
      pattern: "LayerChipRow"
---

<objective>
Deliver the layers + explode + inspect gesture slice: ModelViewer gains a group-scan callback, ExplodeController animates explode in useFrame, LayerChipRow shows layer toggle chips, BottomToolbar enables Layers button and wires chip row, useGestureInterpreter adds spread/fist detection gated on inspectMode.

Purpose: Completes MDL-04 (layer toggles) and MDL-05 (explode view) with gesture binding (D-20 to D-24), delivering the full educational feature set.
Output: ExplodeController.tsx (new), LayerChipRow.tsx (new), ModelViewer.tsx (onGroupsScanned added), BottomToolbar.tsx (Layers button functional + LayerChipRow wired), useGestureInterpreter.ts (spread/fist paths added), Canvas.tsx (ExplodeController mounted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-educational-features/03-CONTEXT.md
@.planning/phases/03-educational-features/03-UI-SPEC.md
@.planning/phases/03-educational-features/03-RESEARCH.md
@.planning/phases/03-educational-features/03-PATTERNS.md
@.planning/phases/03-educational-features/03-01-SUMMARY.md
@.planning/phases/03-educational-features/03-02-SUMMARY.md

<interfaces>
Explode animation (RESEARCH.md Pattern 4):
  EXPLODE_MULTIPLIER = 1.2 (Leva tunable, dev only — per D-19)
  Framerate-independent lerp factor: 1 - Math.pow(0.02, delta)
  explodeProgressRef lerps toward 0.0 (rest) or 1.0 (exploded)
  Per group: position.lerpVectors(restPosition, explodedPosition, explodeProgressRef.current)
  Offset direction: normalize(groupCenter - modelCenter) in LOCAL space (RESEARCH.md Pitfall 5)
  Exploded position: restPosition + direction * boundingRadius * EXPLODE_MULTIPLIER

Layer visibility (RESEARCH.md anti-pattern):
  Use useEffect watching visibleLayers — NOT useFrame
  group.traverse: set child.visible = visibleLayers.has(child.name) for named direct-ish children

Group scan (RESEARCH.md Pattern 3 + Pitfall 3):
  SkeletonPreview hierarchy: modelGroupRef → outer group → ProceduralSkeleton (unnamed) → skull, spine...
  scanNamedGroups must traverse to find ALL named children, not just direct children of modelGroupRef
  Use group.traverse() and collect child.name when child.name is non-empty
  Deduplicate with [...new Set(names)]

Spread gesture (CONTEXT.md D-21, RESEARCH.md Pattern 6):
  isSpread: hand[8].y<hand[6].y && hand[12].y<hand[10].y && hand[16].y<hand[14].y && hand[20].y<hand[18].y
  isFist:   hand[8].y>hand[6].y && hand[12].y>hand[10].y && hand[16].y>hand[14].y && hand[20].y>hand[18].y
  Gate on: useAppStore.getState().inspectMode (non-reactive read — same getState() pattern as A1 assumption)
  If inspectMode && isSpread(hand0): setExplodeActive(true)
  If inspectMode && isFist(hand0):   setExplodeActive(false)

Layer chip UI-SPEC (Surface 3):
  Container: fixed, bottom 44px, left 0, right 0, zIndex 10, overflow-x auto
  background rgba(17,24,39,0.7), backdropFilter blur(4px), padding 8px 16px, minHeight 44px
  Chip active:   background #2563EB, color #fff, no border, borderRadius 16, padding 4px 8px, height 28, fontSize 12
  Chip inactive: background rgba(255,255,255,0.08), color rgba(255,255,255,0.6), border 1px solid rgba(255,255,255,0.2)
  Chip labels: prettify (capitalize first char of each word, replace hyphens with spaces)
  D-14: if availableLayers.length === 0, Layers button disabled (opacity 0.4, cursor not-allowed)

BottomToolbar Layers button:
  Plan 01 stubbed it as disabled. Plan 03 makes it functional.
  Layers button onClick: toggle local layersOpen state (useState in BottomToolbar)
  When layersOpen && availableLayers.length > 0: render LayerChipRow above toolbar
  When availableLayers.length === 0: button disabled (Plan 01 already set this, confirm it's correct)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add onGroupsScanned to ModelViewer and create ExplodeController</name>
  <files>
    src/components/ModelViewer.tsx,
    src/components/ExplodeController.tsx
  </files>
  <read_first>
    - src/components/ModelViewer.tsx — read full file; understand GLBModel and SkeletonPreview rendering paths; understand useLayoutEffect auto-fit pattern to add group scan in same effect
    - src/components/SkeletonPreview.tsx — confirm group hierarchy: outer group ref → ProceduralSkeleton unnamed group → named groups (skull, spine, etc.)
    - .planning/phases/03-educational-features/03-RESEARCH.md — Pattern 3 (group scan), Pitfall 3 (SkeletonPreview depth), Pattern 4 (explode lerp), Pitfall 5 (local vs world space for offsets)
    - .planning/phases/03-educational-features/03-PATTERNS.md — "src/components/ModelViewer.tsx" and "src/components/SceneController.tsx (ExplodeController)" sections
    - src/components/SceneController.tsx — useFrame guard + return null pattern to replicate
    - src/store/appState.ts — confirm setAvailableLayers, setVisibleLayers field names after Plan 01
  </read_first>
  <action>
    Step A — ModelViewer.tsx changes:
    Add an onGroupsScanned callback prop to both GLBModel and SkeletonPreview sub-components:
      onGroupsScanned?: (names: string[]) => void

    Add a module-level scanNamedGroups helper:
      function scanNamedGroups(group: THREE.Group): string[]
        Traverse ALL descendants, collect any child.name that is a non-empty string.
        Use group.traverse((child) => { if (child.name) names.push(child.name) }).
        Deduplicate: return [...new Set(names)].

    In GLBModel: add onGroupsScanned to props and call it from the existing useLayoutEffect,
    after the auto-fit code:
      const names = scanNamedGroups(groupRef.current)
      onGroupsScanned?.(names)

    In SkeletonPreview: add onGroupsScanned to SkeletonPreviewProps and forward it to the group's
    onPointerDown is not the right place — add a separate useLayoutEffect that fires when the
    component mounts (no dependency on clonedScene since SkeletonPreview is procedural):
      useLayoutEffect(() => {
        if (!groupRef.current) return
        const names = scanNamedGroups(groupRef.current)
        onGroupsScanned?.(names)
      }, []) // empty deps — procedural skeleton never changes

    In the top-level ModelViewer: add onGroupsScanned prop; wire it to the correct child.
    The onGroupsScanned callback in ModelViewer's parent (Canvas.tsx) needs to call Zustand:
      setAvailableLayers(names)
      setVisibleLayers(new Set(names))  // D-13: all layers visible on load
    Add these selectors to Canvas.tsx in Task 2 (not here).
    For now, ModelViewer accepts and forwards the prop.

    Also import Box3, Vector3, Group from 'three' if not already present (for scanNamedGroups typing).

    Step B — ExplodeController.tsx:
    Create src/components/ExplodeController.tsx.
    Props: modelGroupRef: React.RefObject&lt;THREE.Group | null&gt;
    Imports: useRef, useEffect from 'react'; useFrame from '@react-three/fiber'; Box3, Vector3, MathUtils, type Object3D from 'three'; useControls from 'leva'; useAppStore from '@/store/appState'.

    Leva controls (top-level unconditional per RESEARCH.md Pitfall 6):
      const { EXPLODE_MULTIPLIER } = useControls('Explode', { EXPLODE_MULTIPLIER: { value: 1.2, min: 0.5, max: 3.0, step: 0.1 } })

    Zustand selectors: explodeActive, availableLayers.

    Type:
      type GroupExplodeData = { name: string; object: Object3D; restPosition: Vector3; explodedPosition: Vector3 }

    Refs:
      groupDataRef = useRef&lt;GroupExplodeData[]&gt;([])
      explodeProgressRef = useRef&lt;number&gt;(0)

    useEffect (recompute group data when model or multiplier changes — deps: availableLayers, EXPLODE_MULTIPLIER):
      const group = modelGroupRef.current
      if (!group || availableLayers.length === 0) { groupDataRef.current = []; return }

      Compute modelCenter by calling new Box3().setFromObject(group).getCenter(new Vector3()).
      Compute boundingRadius = new Box3().setFromObject(group).getSize(new Vector3()).length() / 2.

      Build groupDataRef.current by traversing group to find objects whose name is in availableLayers:
        group.traverse((child) => {
          if (!availableLayers.includes(child.name)) return
          const groupCenter = new Vector3()
          new Box3().setFromObject(child).getCenter(groupCenter)
          const direction = groupCenter.clone().sub(modelCenter).normalize()
          const restPos = child.position.clone()
          const explodedPos = restPos.clone().add(direction.multiplyScalar(boundingRadius * EXPLODE_MULTIPLIER))
          groupDataRef.current.push({ name: child.name, object: child, restPosition: restPos, explodedPosition: explodedPos })
        })

      Note: Positions are computed in LOCAL space (child.position = local space) — correct per RESEARCH.md Pitfall 5.
      The direction is derived from world-space bounding box centers but the offset is applied to the local position — this is acceptable because modelGroupRef is at world origin after auto-fit.

    useEffect for layer visibility (deps: visibleLayers — get visibleLayers selector too):
      const group = modelGroupRef.current
      if (!group) return
      group.traverse((child) => {
        if (child.name && availableLayers.includes(child.name)) {
          child.visible = visibleLayers.has(child.name)
        }
      })

    Add visibleLayers selector from useAppStore.

    useFrame (_, delta):
      if (groupDataRef.current.length === 0) return
      const target = explodeActive ? 1.0 : 0.0
      explodeProgressRef.current = MathUtils.lerp(explodeProgressRef.current, target, 1 - Math.pow(0.02, delta))
      for (const data of groupDataRef.current) {
        data.object.position.lerpVectors(data.restPosition, data.explodedPosition, explodeProgressRef.current)
      }

    Return null (invisible controller).
    Export named function ExplodeController.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && grep -c "onGroupsScanned\|scanNamedGroups" src/components/ModelViewer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep count is 2+ (scanNamedGroups function defined, onGroupsScanned prop used)
    - ExplodeController.tsx exports ExplodeController function
    - grep "EXPLODE_MULTIPLIER" src/components/ExplodeController.tsx returns a match (Leva control present)
    - grep "lerpVectors\|MathUtils.lerp" src/components/ExplodeController.tsx returns matches (lerp animation present)
    - grep "useEffect" src/components/ExplodeController.tsx returns 2+ matches (group data effect + visibility effect)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ModelViewer scans named groups on load and fires onGroupsScanned; ExplodeController animates explode/rest lerp in useFrame and controls layer visibility via useEffect; TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create LayerChipRow, enable Layers button, wire ExplodeController in Canvas</name>
  <files>
    src/components/LayerChipRow.tsx,
    src/components/BottomToolbar.tsx,
    src/components/Canvas.tsx
  </files>
  <read_first>
    - src/components/BottomToolbar.tsx — read current file (after Plan 01 modifications); find the disabled Layers button stub and the local state structure
    - src/components/Canvas.tsx — read current file (after Plan 02 modifications); understand how SceneController and PointerRaycaster are composed; add ExplodeController and onGroupsScanned wiring
    - .planning/phases/03-educational-features/03-UI-SPEC.md — Surface 3 (LayerChipRow) and Surface 4 (toolbar Layers button enabled state)
    - .planning/phases/03-educational-features/03-PATTERNS.md — "src/components/LayerChipRow.tsx" section for chip render pattern
    - src/store/appState.ts — confirm availableLayers, visibleLayers, setVisibleLayers field names
  </read_first>
  <action>
    Step A — LayerChipRow.tsx:
    Create src/components/LayerChipRow.tsx.
    Reads: availableLayers, visibleLayers, setVisibleLayers from useAppStore (individual selectors).
    If availableLayers.length === 0, return null (D-14).

    toggleLayer helper: create a new Set from visibleLayers, toggle the name, call setVisibleLayers(next).

    prettifyLayerName: replace /-/g with ' ', capitalize first letter of each word.

    Render: a fixed div positioned bottom 44px (above toolbar height 44px), left 0, right 0, zIndex 10.
    Container styles: display flex, flexDirection row, flexWrap nowrap, overflowX auto, gap 8, padding 8px 16px, minHeight 44, alignItems center, background rgba(17,24,39,0.7), backdropFilter blur(4px).

    Per chip (availableLayers.map):
      button key={name}
      onClick={() => toggleLayer(name)}
      height 28, borderRadius 16, fontSize 12, fontWeight 400, padding 4px 8px, cursor pointer
      Active (visibleLayers.has(name)): background #2563EB, color #fff, border none
      Inactive: background rgba(255,255,255,0.08), color rgba(255,255,255,0.6), border 1px solid rgba(255,255,255,0.2)
      Label: prettifyLayerName(name)

    Export named function LayerChipRow.

    Step B — BottomToolbar.tsx updates:
    Read the current file after Plan 01. Changes:
    1. Add local state: const [layersOpen, setLayersOpen] = useState(false)
    2. Add selector: const availableLayers = useAppStore((s) => s.availableLayers)
    3. Import LayerChipRow from './LayerChipRow'
    4. Replace the disabled Layers button stub with a functional button:
       - Disabled when availableLayers.length === 0: opacity 0.4, cursor not-allowed, pointerEvents none (or disabled attr)
       - Enabled when availableLayers.length > 0: ghost style, onClick={() => setLayersOpen(!layersOpen)}
       - When layersOpen: add accent border to indicate open state (border: '1px solid #2563EB')
    5. Render LayerChipRow conditionally: {layersOpen && availableLayers.length > 0 && &lt;LayerChipRow /&gt;}
       Place this ABOVE the toolbar bar div (sibling, not inside it), inside the fragment.

    Step C — Canvas.tsx updates:
    Read the current file after Plan 02. Changes:
    1. Import ExplodeController from './ExplodeController'
    2. Add Zustand selectors: setAvailableLayers, setVisibleLayers from useAppStore
       (Canvas needs these to build the onGroupsScanned callback)
    3. Create the onGroupsScanned callback using useCallback:
       const onGroupsScanned = useCallback((names: string[]) => {
         setAvailableLayers(names)
         setVisibleLayers(new Set(names)) // D-13: default all visible on load
       }, [setAvailableLayers, setVisibleLayers])
    4. Pass onGroupsScanned to ModelViewer:
       &lt;ModelViewer controlsRef={controlsRef} modelGroupRef={modelGroupRef} onGroupsScanned={onGroupsScanned} /&gt;
    5. Add ExplodeController inside R3FCanvas, alongside SceneController and PointerRaycaster:
       &lt;ExplodeController modelGroupRef={modelGroupRef} /&gt;
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && grep -c "LayerChipRow\|ExplodeController\|onGroupsScanned" src/components/Canvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep count is 3 (LayerChipRow indirectly via BottomToolbar is OK; ExplodeController rendered; onGroupsScanned callback present)
    - grep "ExplodeController" src/components/Canvas.tsx returns a match
    - grep "onGroupsScanned" src/components/Canvas.tsx returns a match
    - LayerChipRow.tsx exports LayerChipRow function
    - grep "layersOpen\|LayerChipRow" src/components/BottomToolbar.tsx returns matches
    - grep "availableLayers.length === 0" src/components/BottomToolbar.tsx returns a match (disabled state logic)
    - pnpm exec tsc --noEmit exits 0
    - pnpm run build exits 0
  </acceptance_criteria>
  <done>LayerChipRow renders chips for all named groups; Layers button enables/disables based on model; ExplodeController mounted in Canvas; Canvas passes onGroupsScanned to ModelViewer so layers are populated on load.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add spread/fist gesture detection to useGestureInterpreter</name>
  <files>
    src/hooks/useGestureInterpreter.ts
  </files>
  <read_first>
    - src/hooks/useGestureInterpreter.ts — read full current file (after Plan 02 modifications); understand the interpret() callback structure, existing refs, and the useCallback dependency array at the bottom
    - .planning/phases/03-educational-features/03-RESEARCH.md — Pattern 6 (spread/fist detection), anti-pattern note on getState() for non-reactive reads in callbacks
    - .planning/phases/03-educational-features/03-PATTERNS.md — "src/hooks/useGestureInterpreter.ts" gating pattern for spread/fist
    - src/store/appState.ts — confirm setExplodeActive field name after Plan 01
  </read_first>
  <action>
    The hook already has isPointing() from Plan 02. This task adds isSpread() and isFist().
    Read the full current file before making any changes.

    Changes to make:

    1. Add setExplodeActive selector at hook body top (alongside existing setGestureActive and setSelectedMeshName):
       const setExplodeActive = useAppStore((s) => s.setExplodeActive)

    2. Add two helper functions above the interpret useCallback (alongside isPointing):
       function isSpread(hand: NormalizedLandmark[]): boolean
         return hand[8].y < hand[6].y && hand[12].y < hand[10].y && hand[16].y < hand[14].y && hand[20].y < hand[18].y
       function isFist(hand: NormalizedLandmark[]): boolean
         return hand[8].y > hand[6].y && hand[12].y > hand[10].y && hand[16].y > hand[14].y && hand[20].y > hand[18].y

    3. Inside the interpret callback, in the "single hand visible" section (after the pointing detection
       block from Plan 02, before the pinch detection returns), add the spread/fist gate:
       const inspectMode = useAppStore.getState().inspectMode   // non-reactive read (D-22, A1 pattern)
       if (inspectMode) {
         if (isSpread(hand0)) setExplodeActive(true)
         if (isFist(hand0))   setExplodeActive(false)
       }

    4. Add setExplodeActive to the interpret useCallback dependency array:
       [setGestureActive, setSelectedMeshName, setExplodeActive, PINCH_ENTER, PINCH_EXIT, DEAD_ZONE_PX, rotationSensitivity]

    Important:
    - The spread/fist detection block must be BEFORE the pinch detection logic to avoid interference.
      Spread (all fingers extended) is NOT a pinch (thumb+index close), so there is no conflict with
      existing pinch detection — but place it before the pinch block to be safe and consistent.
    - The gating on inspectMode ensures D-22: outside Inspect mode, spread/fist have no explode effect.
    - Pinch+drag rotation continues to work normally in Inspect mode (D-23) because no change is made
      to the pinch detection code paths.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && grep -c "isSpread\|isFist\|inspectMode\|setExplodeActive" src/hooks/useGestureInterpreter.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep count is 4+ (isSpread defined, isFist defined, inspectMode read via getState, setExplodeActive called)
    - grep "useAppStore.getState().inspectMode" src/hooks/useGestureInterpreter.ts returns a match (non-reactive pattern)
    - isPointing from Plan 02 still present (grep "isPointing" src/hooks/useGestureInterpreter.ts)
    - The dependency array includes setExplodeActive (grep "setExplodeActive" src/hooks/useGestureInterpreter.ts returns 2+ matches: selector + dep array)
    - pnpm exec tsc --noEmit exits 0
    - pnpm run build exits 0
  </acceptance_criteria>
  <done>Spread/fist gestures detected in interpret(); gated on inspectMode; setExplodeActive called correctly; existing pinch+drag unaffected; TypeScript and build succeed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaPipe gesture → Zustand state | isSpread/isFist writes to explodeActive via setExplodeActive; bounded to boolean toggle only |
| Three.js group traversal → visibleLayers | Named mesh visibility controlled by Set membership; names come from scene graph, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | ExplodeController useFrame | mitigate | Early return when groupDataRef.current.length === 0; position lerp is bounded math with no allocations per frame |
| T-03-06 | Denial of Service | useEffect layer visibility traversal | accept | Fires only on visibleLayers state change (not per frame); O(n) traverse over mesh nodes is negligible |
| T-03-07 | Spoofing | Spread/fist gesture false positives | accept | False positives toggle a cosmetic 3D effect only; no data or security surface affected |
</threat_model>

<verification>
After completing all three tasks:
1. `pnpm run build` exits 0
2. `pnpm exec tsc --noEmit` exits 0
3. Load the procedural skeleton — Layers button becomes enabled; clicking it shows chip row with skull, spine, ribcage, pelvis, left-arm, right-arm, left-leg, right-leg chips
4. Toggle a chip — the corresponding skeleton part disappears/reappears in the 3D viewport
5. Click Explode — skeleton parts smoothly animate outward over ~0.5s
6. Click Explode again — parts animate back to rest positions
7. Click Inspect — button turns blue; spread fingers gesture triggers explode; fist closes it
8. Pinch+drag rotation works normally while Inspect is active
9. Load body.glb — Layers button is disabled (no named groups); Explode button disabled or has no effect
10. Switch back to skeleton — layer state resets (all chips active again)
</verification>

<success_criteria>
- MDL-04 layer toggles functional: chips auto-generated from model groups, visibility controlled
- MDL-05 explode view functional: smooth animation, magnitude ~1.2× bounding radius
- D-20/D-21/D-22/D-23/D-24 Inspect mode gestures working correctly with correct gating
- D-14 Layers button disabled when no named groups
- D-13 layer state resets on model load
- All Phase 3 requirements (MDL-02, MDL-03, MDL-04, MDL-05) fully delivered across Plans 01–03
- TypeScript and build succeed
</success_criteria>

<output>
After completion, create `.planning/phases/03-educational-features/03-03-SUMMARY.md`
</output>
