---
phase: 02-3d-models-hand-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/store/appState.ts
  - src/types/gestures.ts
  - src/App.tsx
  - public/mediapipe/wasm/
  - public/models/
autonomous: true
requirements: [CAM-03, MDL-01, GEST-01, GEST-02]

must_haves:
  truths:
    - "@mediapipe/tasks-vision and leva are installed and importable"
    - "Zustand store has all Phase 2 state fields (modelUrl, gestureActive, landmarksVisible, handDetected, handTrackingReady, modelLoadError)"
    - "GestureCommand discriminated union and GestureState types exist in src/types/gestures.ts"
    - "App.tsx layer stack comment updated: video z:0, landmark canvas z:1 (placeholder comment), R3F canvas z:2, UI z:10"
    - "MediaPipe WASM files are in public/mediapipe/wasm/ for offline Tauri support"
    - "A second bundled anatomy GLB model (human body or organ) exists in public/models/"
  artifacts:
    - path: "src/store/appState.ts"
      provides: "Extended Zustand store with Phase 2 state"
      contains: "modelUrl"
    - path: "src/types/gestures.ts"
      provides: "GestureCommand, GestureState, GestureMode types"
      exports: ["GestureCommand", "GestureState", "GestureMode"]
    - path: "src/App.tsx"
      provides: "Updated layer stack with placeholder slots for LandmarkCanvas and toolbar"
      contains: "zIndex: 2"
    - path: "public/mediapipe/wasm/"
      provides: "Offline WASM assets for MediaPipe hand tracking"
  key_links:
    - from: "src/store/appState.ts"
      to: "all Phase 2 components"
      via: "useAppStore selectors"
      pattern: "modelUrl|gestureActive|landmarksVisible|handDetected|handTrackingReady"
    - from: "public/mediapipe/wasm/"
      to: "src/hooks/useHandTracking.ts (plan 03)"
      via: "FilesetResolver.forVisionTasks('/mediapipe/wasm')"
      pattern: "/mediapipe/wasm"
---

<objective>
Foundation for Phase 2: install missing dependencies, extend the Zustand store with all Phase 2 state, define gesture type contracts, update App.tsx layer stack to accommodate the landmark canvas, and stage offline MediaPipe WASM assets and a second bundled anatomy model.

Purpose: Every subsequent Phase 2 plan depends on these contracts and assets existing. Plans 02-02 and 02-03 run in parallel after this plan completes.
Output: Installable packages, extended store, typed gesture contracts, updated App.tsx z-index layer stack, offline MediaPipe WASM files in public/, second bundled GLB in public/models/.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-3d-models-hand-tracking/02-CONTEXT.md
@.planning/phases/02-3d-models-hand-tracking/02-RESEARCH.md
@.planning/phases/02-3d-models-hand-tracking/02-UI-SPEC.md
@.planning/phases/02-3d-models-hand-tracking/02-PATTERNS.md

<interfaces>
From src/store/appState.ts (current — extend additively, never replace):
```typescript
export type PermissionState = 'granted' | 'denied' | 'pending' | 'unknown';
interface AppState {
  permissionState: PermissionState;
  setPermissionState: (state: PermissionState) => void;
}
export const useAppStore = create<AppState>((set) => ({ ... }));
```

From src/App.tsx (current — update z-indices):
```tsx
// z:0  — video background (WebcamProvider)
// z:1  — R3F Canvas <div style={{ zIndex: 1 }}>
// z:10 — UI overlays (comment placeholder)
```

Target layer stack after this plan:
```tsx
// z:0   — video background (WebcamProvider)
// z:1   — LandmarkCanvas (placeholder slot, not yet mounted — comment only)
// z:2   — R3F Canvas (bumped from z:1 to accommodate integer landmark stack)
// z:10  — UI overlays (HandStatusIndicator, BottomToolbar — placeholder comments)
```

From 02-PATTERNS.md (gesture types to create):
```typescript
// src/types/gestures.ts
export type GestureMode = 'idle' | 'pinching' | 'two-hand-pinch';
export interface GestureState {
  mode: GestureMode;
  pinchOrigin: { x: number; y: number } | null;
}
export type GestureCommand =
  | { type: 'idle' }
  | { type: 'rotate'; delta: { x: number; y: number } }
  | { type: 'scale'; factor: number }
  | { type: 'pan'; delta: { x: number; y: number } };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and extend Zustand store with Phase 2 state</name>
  <files>package.json, pnpm-lock.yaml, src/store/appState.ts, src/types/gestures.ts</files>
  <read_first>
    - src/store/appState.ts — read the full file before editing; add to it, never replace
    - src/App.tsx — read before editing in Task 2
    - 02-PATTERNS.md sections: appState.ts, gestures.ts — exact field names and types
    - 02-RESEARCH.md section: Standard Stack — exact package versions
  </read_first>
  <action>
    Run: pnpm add @mediapipe/tasks-vision leva

    Verify both packages install without errors. Expected: @mediapipe/tasks-vision@0.10.35, leva@0.10.1.

    Create src/types/ directory if it does not exist, then create src/types/gestures.ts with the four exports: GestureMode (union 'idle' | 'pinching' | 'two-hand-pinch'), GestureState (interface with mode and pinchOrigin fields), GestureCommand (discriminated union with idle/rotate/scale/pan variants). Use the exact shapes from the interfaces block above. No implementation code — types only.

    Extend src/store/appState.ts additively: add six new fields to the AppState interface and matching initial values + setters to the create() call. Fields to add (per D-01 through D-16 decisions and 02-PATTERNS.md):
    - modelUrl: string | null — null means procedural skeleton is visible (D-01)
    - setModelUrl: (url: string | null) => void
    - gestureActive: boolean — true disables OrbitControls (D-23), initial false
    - setGestureActive: (v: boolean) => void
    - landmarksVisible: boolean — D-12 toggle, initial true
    - setLandmarksVisible: (v: boolean) => void
    - handDetected: boolean — D-10 status indicator, initial false
    - setHandDetected: (v: boolean) => void
    - handTrackingReady: boolean — D-16 non-blocking load, initial false
    - setHandTrackingReady: (v: boolean) => void
    - modelLoadError: string | null — D-08 toast message, initial null
    - setModelLoadError: (msg: string | null) => void

    Each setter follows the exact existing pattern: (v) => set({ fieldName: v }). Do not change any existing Phase 1 fields. The import line (import { create } from 'zustand') remains unchanged.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - pnpm tsc --noEmit exits 0 (no TypeScript errors)
    - node -e "require('./node_modules/@mediapipe/tasks-vision/package.json')" exits 0
    - src/types/gestures.ts exists and exports GestureCommand, GestureState, GestureMode
    - src/store/appState.ts contains all six new Phase 2 fields with correct TypeScript types
    - No Phase 1 fields (permissionState, setPermissionState) were changed or removed
  </acceptance_criteria>
  <done>Dependencies installed, type contracts defined, store extended — downstream plans can import from both without modification.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx layer stack and stage MediaPipe offline assets</name>
  <files>src/App.tsx, public/mediapipe/wasm/ (directory + files), public/models/</files>
  <read_first>
    - src/App.tsx — read full file before editing
    - 02-RESEARCH.md section: Pitfall C (MediaPipe CDN in Tauri) — offline WASM copy instructions
    - 02-RESEARCH.md section: Pattern 4 (FilesetResolver path) — target path '/mediapipe/wasm'
    - 02-UI-SPEC.md section: Layer Stacking Order — exact z-index values
    - 02-CONTEXT.md D-04 — bundled anatomy models requirement
  </read_first>
  <action>
    Update src/App.tsx to establish the Phase 2 layer stack with comment placeholders for components not yet built. Update the R3F canvas wrapper div to zIndex: 2 (the landmark canvas uses integer z:1, so R3F must shift to z:2 for proper stacking; this is a breaking change from Phase 1 z:1 assignment — update the existing div). Add comment placeholders in JSX for: LandmarkCanvas (will be added in Plan 02-03, z:1 integer), HandStatusIndicator (Plan 02-03, zIndex:10), BottomToolbar (Plan 02-02, zIndex:10). Do not import these components yet — they don't exist. Use {/* TODO Plan 02-NN: <ComponentName /> */} comments as placeholders. Update the z:1 comment to note the R3F canvas is now above the planned landmark canvas.

    Copy MediaPipe WASM files for offline Tauri support (Pitfall C in RESEARCH.md): run the following to download the WASM bundle from the npm package into public/mediapipe/wasm/:
      node -e "
        const fs = require('fs');
        const path = require('path');
        const src = path.join('node_modules', '@mediapipe', 'tasks-vision', 'wasm');
        const dst = path.join('public', 'mediapipe', 'wasm');
        fs.mkdirSync(dst, { recursive: true });
        fs.readdirSync(src).forEach(f => fs.copyFileSync(path.join(src, f), path.join(dst, f)));
        console.log('Copied', fs.readdirSync(dst).length, 'files');
      "

    Also copy the hand_landmarker.task model file into public/mediapipe/:
      node -e "
        const fs = require('fs');
        const path = require('path');
        fs.mkdirSync('public/mediapipe', { recursive: true });
        const taskSrc = path.join('node_modules', '@mediapipe', 'tasks-vision');
        const files = fs.readdirSync(taskSrc).filter(f => f.endsWith('.task'));
        if (files.length) {
          fs.copyFileSync(path.join(taskSrc, files[0]), path.join('public', 'mediapipe', files[0]));
          console.log('Copied task file:', files[0]);
        } else {
          console.log('No .task file in package root — will use CDN fallback');
        }
      "

    If the .task model file is not bundled in the npm package (it may not be), note this in the plan summary and the useHandTracking hook (Plan 02-03) will download it from the Google CDN at runtime. The WASM files (the larger download) being local is still the primary Tauri offline benefit.

    For the second bundled anatomy GLB (D-04): check public/models/ for what exists (skeleton.glb is confirmed present). Search for a second model:
    1. Run: ls public/models/ to confirm skeleton.glb exists
    2. Download a CC0 anatomy GLB and save it as public/models/body.glb. Required — do not skip. Target: Sketchfab human skeleton CC0 or equivalent. Attempt the following sources in order: (a) https://raw.githubusercontent.com/KhronosGroup/glTF-Sample-Models/main/2.0/RiggedFigure/glTF-Binary/RiggedFigure.glb (CC0, Khronos group) or (b) search Sketchfab API at https://api.sketchfab.com/v3/models?q=skeleton&license=cc0&downloadable=true&type=models and attempt download of the first freely downloadable CC0 result. Verify: ls -lh public/models/body.glb shows file size > 10KB. If both automated downloads fail (authentication required, network error), the executor must report this explicitly in the plan summary with the exact error — the acceptance criterion requires a real GLB file in public/models/body.glb before this plan is marked complete.
  </action>
  <verify>
    <automated>ls public/mediapipe/wasm/ | head -5 && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - public/mediapipe/wasm/ exists and contains at least 2 files (WASM binary + .js loader)
    - src/App.tsx renders without TypeScript errors (pnpm tsc --noEmit passes)
    - src/App.tsx contains comment placeholders for LandmarkCanvas, HandStatusIndicator, BottomToolbar
    - src/App.tsx z-index comment for R3F canvas is updated to reflect Phase 2 stack
    - public/models/skeleton.glb exists (confirmed present from Phase 1)
    - public/models/body.glb exists (a real CC0 anatomy GLB file) — executor must download this during task execution and verify the file is a valid binary (file size > 10KB). A placeholder .txt file is NOT acceptable as the capstone demo requires real 3D content.
  </acceptance_criteria>
  <done>Layer stack is documented and ready for Phase 2 components. MediaPipe WASM is staged locally for offline Tauri builds. Foundation complete — Plans 02-02 and 02-03 can proceed in parallel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file system → app | User-provided GLB files loaded via file picker (Plan 02-02) |
| CDN → Tauri webview | MediaPipe WASM downloaded from jsdelivr if local copy absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | GLB file loading (Plan 02-02) | mitigate | Wrap useGLTF in ErrorBoundary; on exception show toast (D-08), retain previous model — never execute user-supplied binary |
| T-02-02 | Denial of Service | Object URL memory leak | mitigate | Track prevObjectUrlRef, call URL.revokeObjectURL(prevUrl) after new model loads successfully (Pitfall D) |
| T-02-03 | Information Disclosure | MediaPipe CDN fallback | accept | WASM is public Google code; no PII transmitted; local copy is primary mitigation for Tauri |
</threat_model>

<verification>
pnpm tsc --noEmit exits 0
pnpm dev starts without console errors
public/mediapipe/wasm/ contains WASM binary files
src/types/gestures.ts exports GestureCommand, GestureState, GestureMode
src/store/appState.ts contains modelUrl, gestureActive, landmarksVisible, handDetected, handTrackingReady, modelLoadError fields
</verification>

<success_criteria>
- TypeScript compiles cleanly after store extension
- All six Phase 2 state fields accessible via useAppStore selectors in any component
- MediaPipe WASM staged locally — hand tracking hook (Plan 02-03) can use '/mediapipe/wasm' path
- App.tsx layer stack comment accurately reflects Phase 2 component slots
</success_criteria>

<output>
After completion, create .planning/phases/02-3d-models-hand-tracking/02-01-SUMMARY.md
</output>
